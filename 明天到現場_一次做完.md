# 明天到 Win10 現場 · 一次做完清單

> 目標：做完這一趟，之後**所有事情都能遠端處理**，不用再跑第二次。
> 預估 20 分鐘。照順序做，每步都有「怎麼知道成功了」。

---

## ① AnyDesk 裝成服務 ★最重要

**這步做完，以後鎖定畫面也進得去。**

1. 登入 Windows
2. 打開 AnyDesk
3. 找「**安裝 AnyDesk 到此電腦**」（Install AnyDesk on this computer）
   - 通常在主畫面中間，或右上角三條線選單裡
4. 點下去，一路確認
5. 設定 → 安全性 → **啟用無人值守存取**，設一組密碼（記下來）
6. 同一頁 → **取消勾選「工作階段結束後鎖定帳戶」** ← 就是這個害您上次卡住
7. **重開機**

✅ **怎麼知道成功**：重開機後，AnyDesk 的「動作」選單裡
「重新啟動遠端設備」不再是灰色的。

---

## ② 自動登入

停電復電後才不會卡在登入畫面。

1. `Win + R` → 輸入 `netplwiz` → Enter
2. **取消勾選**「使用者必須輸入使用者名稱和密碼」
3. 套用 → 輸入密碼兩次 → 確定

✅ **怎麼知道成功**：重開機後直接進桌面，不用打密碼。

---

## ③ 解壓縮工具包

1. 把 `win7pack.zip` 傳過去（AnyDesk 檔案傳輸，或用隨身碟）
2. 解壓縮到 **`C:\kiosk\`**

解完應該長這樣：

```
C:\kiosk\
├── 1-diagnose.bat
├── setup-windows.bat
├── start-bridge.bat
├── start-kiosk.bat
├── build.bat
├── UpusSkb.cs  Program.cs  SkbPanel.cs  SkbBridge.cs  SheetSync.cs
├── sheet.ini   cells.csv   README-kiosk.md
└── web\index.html
```

---

## ④ 跑系統設定

對 `setup-windows.bat` **按右鍵 → 以系統管理員身分執行**

（電源設從不、關螢幕保護、註冊 8080 埠、擋 Windows Update 自動重開機）

✅ 出現「urlacl 已存在」的錯誤是**正常的**，不用管。

---

## ⑤ 485 接線：把綠線和紅線對調 ★

**趁人在現場，這個一定要做。**

USB-485 轉換器的綠色端子，絲印由左到右是：

```
VCC | GND | GND2 | B | A
```

現在是：黃 → GND2、綠 → B、紅 → A

**把綠線和紅線互換**（綠插 A、紅插 B）。

- 接反不會燒板，這是最安全也最可能的解法
- 如果手邊有三用電表，順便從板子白色 6P 座的 A / B / G 針腳量通斷，
  確認紅黃綠各自對應哪一支——不要靠線色猜

---

## ⑥ 雙擊 `1-diagnose.bat`

跑 1~2 分鐘，畫面出現 **DONE** 就好。

**不用讀畫面上的任何東西。**

它會產生 `C:\kiosk\diag-log.txt`——把這個檔案傳回來就結束了。

### ⚠️ 不用另外跑 build.bat

**編譯已經包在 `1-diagnose.bat` 裡面**，是它的第 5 段。這支一次做完：

| 段 | 做什麼 |
|:--:|---|
| 1 | Windows 版本 |
| 2 | .NET 版本（判斷 TLS 1.2 能不能用 → 影響 Google Sheet 同步） |
| 3 | 列出所有 COM 埠（順便確認到底是 COM3 還是別的） |
| 4 | 列出資料夾內容 |
| 5 | **編譯 SkbProbe.exe 和 SkbBridge.exe** ← 就是這裡 |
| 6 | 模擬器自測（不用接硬體，驗證軟體本身沒問題） |
| 7 | **raw** — 有沒有收到任何位元組 |
| 8 | **sweep** — 掃 5 種波特率 |
| 9 | **probe** — 9600 完整驗板 |

`build.bat` 我放在包裡只是備用（它多編一支 `SkbPanel.exe` 視覺化面板，
排查時用得到）。**明天不用碰它。**

### 如果 COM 埠不是 3

不用先確認。直接跑預設的就好——log 的第 3 段本來就會列出所有 COM 埠，
我看 log 就知道。真的要指定的話，才用命令列打 `1-diagnose.bat COM5`。

---

## 做完之後

回到家用 AnyDesk 連進去（這次鎖定畫面也進得去了），
把 `diag-log.txt` 傳給我，剩下的我遠端處理。

---

## 如果時間不夠，最低限度只做這兩件

1. **① AnyDesk 裝成服務**（沒這個，以後每次都要跑一趟）
2. **⑤ 綠紅線對調**（30 秒，而且很可能直接解決 485 問題）

其他都能遠端補。
