# HANDOFF — 智慧拍櫃 kiosk 專案交接（2026-09-04）

> 給下一個 session 的開局脈絡。讀完這份即可接手,不必重看整段對話。

## 0. 一句話現況
羽拍有約 24h 無人店的「智慧拍櫃」kiosk 已上線運行;**本 session 已把後台改成「保險箱式多員工 PIN 登入 + 3 層權限」、語音改由 kiosk poller 在印貼紙前播、並做了一鍵裝機腳本**。目前待使用者驗證「語音時序 + 30 秒回主選單」是否 OK。

## 1. 系統地圖
| 項目 | 位置 |
|---|---|
| 拍櫃 app(shop) | repo `github.com/HemanJJ/smartlocker`,**branch `root`**,部署 shop.dearfly.com.tw(Vercel+Neon) |
| 訂場 app(booking) | repo `github.com/HemanJJ/Booking`,branch `main`,部署 dearrfly.com.tw |
| kiosk 機器 | Win10,`192.168.1.119`(SSH `Admin`/`123456`,有時離線),Tailscale `100.108.96.124` |
| kiosk 桌機檔案 | `C:\Users\Admin\Desktop\`(poller/語音/啟動器)、`C:\kiosk\kiosk-shell.vbs`(保活 shell) |
| obsidian 知識庫 | `difly-obsidian`(28 篇,來源 `/Users/hohe/Booking/docs/obsidian`)、`difly-kiosk`(kiosk 操作手冊 1 篇) |

## 2. 本 session 完成的大事(依序)
1. **kiosk 基礎穩定**:主 poller/列印/排程全改「隱藏視窗」;鎖控板 RS-485 走 `COM3`(CH340);watchdog 每 1 分鐘保活;console 黑窗已根治(用 wscript+VBS 隱藏啟動)。
2. **語音系統**:16 條 kiosk 語音(拍師傅人設、溫暖文案)、廣告 `ad-1..5` 每 10 分鐘輪播(KioskAds 元件,避開 /admin /store)。
3. **預算下單路**:kiosk 下單多一條「只選預算(NT$250/300/350/400/450)」→ 訂單存 `budget`,後台「指派線種+磅數」(`/api/orders/[id]/assign`)。
4. **後台改保險箱式登入 + 3 層權限**(本 session 後段核心):
   - 登入 = 人名下拉 + 4 碼 PIN 數字鍵盤(不再用文字密碼 → 順便繞掉 Chrome「Google 密碼管理」跳出)。
   - 員工:`管理員`(admin/MIS)、`王清標`、`王小姐`、`謝小姐`、`黃先生`(staff),**預設 PIN 都是 1234**,登入後可自改(`/admin/password` →「改我的 PIN」)。
   - admin 才有:`🔓一鍵全開`、`👥員工管理 /admin/staff`(加人/重置 PIN)、看操作紀錄;staff 只有櫃台作業。
   - `一鍵全開` = admin-only + 密碼=**今日 4 碼日期(如 0904)**+ 記名進 log(`/api/admin/open-all`)。
5. **語音時序修正**:poller「交拍」流程改成 **①等一下/開櫃(wait-open)→②貼上去再放入櫃(anon-order)→③印貼紙→④開格**,兩條用 `PlaySync`(不重疊);網頁綁定的 anon-order/wait-open **已拿掉**(避免重音)。
6. **一鍵裝機**:repo `kiosk/install/setup-kiosk.ps1` + `pack/`(現役 poller/啟動器/16 語音母版)+ README,新機跑一次即還原整個 kiosk。

## 3. 當前關鍵狀態
- **後台登入**:`王清標`=staff、`管理員`=admin(名字若老闆要改,只改 `staff.ts` 的 `SEED_STAFF`)。
- **語音**:kiosk poller(PID 2668,hidden,COM3)播 wait-open(2s)+ anon-order(**13.5s**,偏長,待老闆決定要不要縮短)→ 印 → 開格。
- **時序常數**:`web/src/app/order/page.tsx` 的 `DONE_SECONDS = 30`(綁定後 30 秒回下單頁)。
- **一鍵全開密碼** = 今日日期 4 碼(taiwanMMDD,UTC+8)。

## 4. 待辦 / 下一步(接手注意)
- [ ] 等老闆驗證:語音時序 + 30 秒節奏;若 `anon-order` 13.5s 太長 → 縮短文案重生 wav(用 `say -v Meijia -r 185` → `afconvert -f WAVE -d LEI16@22050`)。
- [ ] 導覽列「🔑 改密碼」字樣 → 統一成「改 PIN」(小改,`AdminNav.tsx`)。
- [ ] 老闆可能想把 admin 帳號「管理員」改名(改 `staff.ts` SEED_STAFF + 既有 DB 的 UPDATE)。
- [ ] 系統 22 格 vs 實體 10 格:老闆**決定維持 22**(虛格 11~22 會開鎖但沒門,先不動)。若之後要降 10,設 `LOCKER_SLOT_COUNT=10` + 清 DB 空格。
- [ ] 格號有 off-by-one(系統格 N = 實體格 N-1),老闆已知、視為正常。

## 5. 踩坑(接手必看,別重蹈)
1. **cookie 讀取**:後台 route 要用 `request.cookies.get(ADMIN_COOKIE)?.value`(會解碼),**不要手動 `req.headers.get('cookie')` 再 split(':')(會被 `%3A` 搞壞 → 誤判「未登入」)**。helper 在 `lib/admin-auth.ts` 的 `getAdminName()`。
2. **Chrome 關密碼管理**:用**registry policy**(`HKLM/HKCU\SOFTWARE\Policies\Google\Chrome` 的 `PasswordManagerEnabled=0`/`BrowserSignin=0`/`Autofill*=0`),**不要用 `--disable-features=PasswordManager` 啟動 flag——會讓 Chrome 152 崩潰**(本 session 踩過)。
3. **SSH 到 kiosk**:expect 腳本**一律多行 expect block**(單行會 hang);中文/特殊字元用 scp 檔案再執行,別 inline 轉義。
4. **語音檔要放兩處**:雲端 `web/public/kiosk-voice/`(網頁播)+ kiosk 本機 `C:\Users\Admin\Desktop\kiosk-voice\`(poller 播),改文字要兩邊都更新。
5. **⚠️ 有另一個 AI 同時在推這個 repo**(UptimeRobot webhook、richmenu 等 commit 就是它),它還曾誤刪 `anon-order.wav`(我已還原)。**push 前先 `git fetch` + merge**,注意衝突。
6. `LayoutProps` 是既有 tsc 錯誤(`layout.tsx`),不是本次引入,`next build` 沒擋(會 deploy),先別管。

## 6. 常用命令/路徑速查
- 部署 smartlocker:`cd /Users/hohe/smartlocker && git push origin root`(Vercel 自動)。
- kiosk 語音重製:`say -v Meijia -r 185 -o /tmp/x.aiff "文案"` → `afconvert -f WAVE -d LEI16@22050 /tmp/x.aiff x.wav`。
- kiosk poller 重啟:殺 `kiosk-print-poller` powershell + 用 `wscript launch-poller-hidden.vbs` 重啟(隱藏)。
- 一鍵全開測試端點:`POST /api/admin/open-all {password:'0904'}`(需 admin cookie)。
- 重建 obsidian RAG:`kb_build`(name=`difly-obsidian`,out=`/Users/hohe/.dsh/skills`,來源 `/Users/hohe/Booking/docs/obsidian`)。
