# CLAUDE.md — smartlocker

## 開場先讀這三份

1. **`HANDOFF.md`** — 目前狀態的**單一真相**。永遠先讀這份
2. **`AGENTS.md`** — 架構分層與硬性規則
3. **`docs/skb-doc.txt`** — 鎖控板協議（⚠️ 見下方警告）

`README.md` 的 RS-485 章節是**時間序日記**，中段有已被推翻的舊假設，
不要拿它當現況。現況以 `HANDOFF.md` 為準。

---

## 這個專案是什麼

24 小時無人取件櫃（Racket Master 智慧拍櫃）。
客人輸入取件碼 → 對應格口自動開啟 → 關門完成。

軟體幾乎都完成了（協議庫、HTTP 橋接、模擬器、Kiosk 前端、Sheet 同步、部署腳本）。
**唯一卡住的是實體 RS-485 通訊。**

---

## 現在卡在哪（2026-08-10）

**兩塊鎖控板對任何指令都完全不回應，串口層已徹底窮盡。**

已證實正常：轉換器（兩支對接自測互通）、線材（萬用表通斷實測）、
匯流排電氣（34.2kΩ 收發晶片阻抗）、板卡 MCU（一鍵全開有效）。

已排除：8 種波特率 × 3 種校驗 × 2 種停止位 = 48 組、位址廣播 + 40 個點名、
A/B 兩種極性、兩塊不同型號板卡、兩台電腦兩種 OS。

**專職監聽器全程沒收到板卡發出的任何一個位元組。**

> ⚠️ **結論：`docs/skb-doc.txt`（《锁控板技术文档 V3.1》）極可能不是這批板子的文件。**
> 實體板卡與文件圖片完全不符（藍板 vs 黑板、多了藍牙/乙太模組空焊區、
> TTL 接口形式不同、50 路板根本不在文件內）。
> **在拿到正確協議文件之前，不要再依 `skb-doc.txt` 調整幀格式，那是在錯誤的前提上優化。**

詳細證據鏈見 `HANDOFF.md`。

---

## 怎麼跟這位使用者合作

**使用者是資深工程師／架構師，但不是韌體或電子背景。** 硬體排查請假設是新手。

- **指令一次給一條**，各自獨立成一個 code block，方便複製貼上
- 每條指令說明「按下去會看到什麼」，以及「成功長什麼樣」
- 硬體動作（換線、量電壓、焊接）要標出**地標**（例如「47µF 電容正下方那顆螺絲」），
  不要只講「左邊第三個」
- **不可逆的動作（焊接、通電接線）之前，先做一次安全確認**
- 以「您」稱呼使用者，自稱「助手」
- 回覆精簡，不要長篇大論

---

## 硬性規則（來自 AGENTS.md，違反會出事）

- **開鎖路徑不得依賴網路。** 取件碼一律查本機快取，斷網必須還能開格
- **`docs/skb-doc.txt` 是協議的唯一真相**（但見上方警告）—— 不要憑記憶寫幀格式
- **`kiosk/*.bat` 只能用 ASCII**，中文在 cmd 會變亂碼
- **`kiosk/web/index.html` 維持單檔零相依**，ES5，目標 Chrome 109
- 不要把客人姓名電話放進 Google Sheet（發布的 CSV 網址是公開的）

---

## 驗板工具

```bash
cd tools
source .venv/bin/activate     # 首次：python3 -m venv .venv && pip install pyserial

python3 skb_probe.py ports    # 列出序列埠 ← 每次先跑這個，埠號會漂
python3 skb_probe.py raw      # 送一次尋址幀
python3 skb_probe.py sweep    # 8 種波特率 × 3 種指令
python3 skb_probe.py ping     # DF 廣播 + DD 1~32 + D0 1~8
python3 skb_probe.py deep     # 48 種串口參數組合
python3 skb_probe.py hunt     # 持續偵測，接對會嗶三聲（換線時用）
python3 skb_probe.py listen 120   # 只聽不送 ← 協議逆向就用這個
```

加 `--port /dev/cu.usbserial-XXXX` 指定埠。

**你（Claude Code）可以直接執行這些指令並讀取輸出**——這是這個環境最大的優勢，
上一個環境只能請使用者複製貼上。善用它：**自己跑、自己讀、自己迭代。**
只有需要人動手的部分（插拔線、量電壓、按按鈕）才請使用者代勞。

---

## 下一步（依優先序）

1. **向廠商取得**：兩塊板卡完整型號、對應協議文件、**廠商的上位機測試軟體（.exe）**
2. **協議逆向**：拿到廠商軟體後，用它開一次鎖，同時第二支轉換器並聯掛在匯流排上跑
   `listen 120`，把真實指令抓下來 → 改寫 `src/UpusSkb.cs` 與 `tools/skb_probe.py`
3. **備案 TTL**：25 路板的 `TTL IO` 是 2×2 空焊盤需焊接。
   ⚠️ 手邊的 USB-TTL 是 **PL2303HX**，新版 macOS 很可能不支援——
   **焊接前先插上跑 `ports` 確認驅動可用**（這一步尚未執行）
4. **MQTT / 樹莓派**：可平行推進，不必等 485。決策已定（見 `HANDOFF.md`）

---

## 手上的硬體

| 項目 | 狀態 |
|---|---|
| 25 路鎖控板（黑 PCB，`IAP15W4K61S4`） | 有電、RESET 有作用 |
| 50 路鎖控板（七段顯示器 + 三鍵設址 + 一鍵全開） | MCU 存活，顯示 `01` |
| USB-485 轉換器 ×2（CH340） | 兩支都正常 |
| USB-TTL（**PL2303HX**） | ⚠️ 驅動相容性未驗證 |
| 三用電表、杜邦線、烙鐵 | 有 |
| Mac mini | 主要開發機 |
| Win7 主機 | **開不了機**，非關鍵路徑 |

**線色定義（萬用表實測，不是推測）**：紅 = 485-A、綠 = 485-B、黃 = 485-GND
